[package]
name = "bpf-program-template"
version = "0.1.0"
edition = "2018"
license = "WTFPL"
publish = false

[dependencies]
solana-program = "1.6.12"

[features]
test-bpf = []

[dev-dependencies]
assert_matches = "1.4.0"

[lib]
crate-type = ["cdylib", "lib"]
